FC = gfortran
FLG = -O2
AR = ar r
RAN = ranlib

DP = -fdefault-real-8
EISLIB = eispack_gDP.a


$(EISLIB): eispack.f
	$(FC) -c $(FLG) $(DP) eispack.f
	$(AR) $(EISLIB) eispack.o
	$(RAN) $(EISLIB)

clean:
	rm *.o *.a

